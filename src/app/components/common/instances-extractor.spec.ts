import {InstancesExtractor} from './instances-extractor';

describe('InstancesExtractor', () => {
  it('should create an instance', () => {
    expect(new InstancesExtractor()).toBeTruthy();
  });
});
