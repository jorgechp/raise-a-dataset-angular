import { AbstractMissionService } from './abstract-mission-service';

describe('AbstractMissionService', () => {
  it('should create an instance', () => {
    expect(new AbstractMissionService()).toBeTruthy();
  });
});
