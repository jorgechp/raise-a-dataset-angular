import { HateoasConfigurationService } from './hateoas-configuration.service.ts';

describe('HateoasConfigurationServiceTs', () => {
  it('should create an instance', () => {
    expect(new HateoasConfigurationService()).toBeTruthy();
  });
});
